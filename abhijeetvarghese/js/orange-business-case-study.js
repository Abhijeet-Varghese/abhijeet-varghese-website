(()=>{"use strict";const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];const reducedMotion=window.matchMedia?.("(prefers-reduced-motion: reduce)")?.matches??false;const hero=$(".ob-hero");if(hero){requestAnimationFrame(()=>requestAnimationFrame(()=>hero.classList.add("is-in")));}
const pins=$$(".ob-pin");const panoNum=$("#panoNum"),panoTitle=$("#panoTitle"),panoDesc=$("#panoDesc");const panoText=$("#panoText");let tourTimer=null,tourIdx=0,tourAllowed=!reducedMotion,userTouchedPano=false;const selectPin=(pin)=>{if(!pin)return;pins.forEach((p)=>p.classList.toggle("is-active",p===pin));if(panoNum)panoNum.textContent=pin.dataset.n;if(panoTitle)panoTitle.textContent=pin.dataset.title;if(panoDesc)panoDesc.textContent=pin.dataset.desc;if(panoText){panoText.classList.remove("is-swap");void panoText.offsetWidth;panoText.classList.add("is-swap");}
pins.forEach((p)=>{if(p!==pin)p.removeAttribute("aria-current");});pin.setAttribute("aria-current","true");};if(pins.length&&panoTitle){pins.forEach((pin)=>pin.addEventListener("click",()=>{userTouchedPano=true;if(tourTimer){clearInterval(tourTimer);tourTimer=null;}
selectPin(pin);}));if(tourAllowed){const panoFig=$(".ob-pano");if(panoFig&&"IntersectionObserver"in window){new IntersectionObserver(([e])=>{if(!e.isIntersecting){if(tourTimer){clearInterval(tourTimer);tourTimer=null;}
return;}
if(userTouchedPano||tourTimer)return;setTimeout(()=>{if(userTouchedPano||tourTimer||document.hidden)return;tourTimer=setInterval(()=>{if(document.hidden)return;tourIdx=(tourIdx+1)%pins.length;selectPin(pins[tourIdx]);},4200);},2200);},{threshold:0.5}).observe(panoFig);}}}
const stageBtns=$$(".ob-stages__list button");const stagePanel=$(".ob-stages__panel");if(stageBtns.length&&stagePanel){const stageBody=$(".ob-stages__body",stagePanel);stageBtns.forEach((btn)=>btn.addEventListener("click",()=>{stageBtns.forEach((b)=>{b.classList.toggle("is-active",b===btn);b.setAttribute("aria-selected",b===btn?"true":"false");});stagePanel.dataset.i=btn.dataset.i;const lis=(btn.dataset.b||"").split("|").filter(Boolean).map((t)=>"<li>"+t+"</li>").join("");stageBody.innerHTML='<span class="ob-stages__tag">'+btn.dataset.tag+"</span>"+"<h3>"+btn.dataset.i+" — "+btn.dataset.title+"</h3>"+"<p>"+btn.dataset.desc+"</p>"+"<ul>"+lis+"</ul>";stageBody.classList.remove("is-swap");void stageBody.offsetWidth;stageBody.classList.add("is-swap");}));}
const room=document.querySelector(".room-response");const roomToggle=document.querySelector(".response-toggle");if(room&&roomToggle){const setRoom=(active)=>{room.dataset.state=active?"active":"standby";const v=room.querySelector(".response-visitor");const cu=room.querySelector(".response-curtains");const li=room.querySelector(".response-lights");const mo=room.querySelector(".response-mode");if(v)v.textContent=active?"DETECTED":"NO VISITOR";if(cu)cu.textContent=active?"CLOSED":"OPEN";if(li)li.textContent=active?"ON":"OFF";if(mo)mo.textContent=active?"ACTIVE":"STANDBY";if(roomToggle.firstChild)roomToggle.firstChild.textContent=active?"LEAVE ROOM ":"ENTER ROOM ";roomToggle.setAttribute("aria-pressed",String(active));roomToggle.setAttribute("aria-label",active?"Set room to standby":"Activate room response");};setRoom(false);roomToggle.addEventListener("click",()=>setRoom(room.dataset.state!=="active"));}
const tabs=$$(".ob-media__tabs button");const frames=$$(".ob-media__frame");const stageEl=$(".ob-media__stage");if(tabs.length&&frames.length){frames.forEach((fr)=>{const src=$("source",fr);const vid=$("video",fr);const mark=()=>$(".ob-media__vid",fr)?.classList.add("is-missing");if(src)src.addEventListener("error",mark);if(vid)vid.addEventListener("error",mark,true);});const activeVideo=()=>$(".ob-media__frame.is-active video");tabs.forEach((tab)=>tab.addEventListener("click",()=>{tabs.forEach((t)=>{t.classList.toggle("is-active",t===tab);t.setAttribute("aria-selected",t===tab?"true":"false");});frames.forEach((fr)=>{const on=fr.id===tab.dataset.target;fr.classList.toggle("is-active",on);const v=$("video",fr);if(v&&!on)v.pause();});const v=activeVideo();if(v){v.load();v.play?.().catch(()=>{});}}));if(stageEl&&"IntersectionObserver"in window){new IntersectionObserver(([e])=>{const v=activeVideo();if(!v)return;if(e.isIntersecting){if(v.readyState===0)v.load();v.play?.().catch(()=>{});}else{v.pause();}},{threshold:0.15}).observe(stageEl);}}
if("IntersectionObserver"in window){const rObs=new IntersectionObserver((entries,obs)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add("is-in");obs.unobserve(entry.target);}});},{threshold:0.16});$$(".reveal, .ob-reveal").forEach((el)=>rObs.observe(el));const closing=$(".ob-closing");if(closing){new IntersectionObserver(([e],obs)=>{if(e.isIntersecting){closing.classList.add("is-in");obs.disconnect();}},{threshold:0.3}).observe(closing);}}
const roleBtns=$$(".role-chain button");const roleOut=$(".role-chain-output p");if(roleBtns.length&&roleOut){roleBtns.forEach((btn)=>btn.addEventListener("click",()=>{roleBtns.forEach((b)=>b.classList.toggle("active",b===btn));roleOut.textContent=btn.dataset.copy||"";}));}
const purposeBtns=$$(".purpose-strip button");if(purposeBtns.length){const exp=$(".purpose-output > div:first-child p");const bus=$(".purpose-output > div:last-child p");purposeBtns.forEach((btn)=>btn.addEventListener("click",()=>{purposeBtns.forEach((b)=>b.classList.toggle("active",b===btn));if(exp)exp.textContent=btn.dataset.experience||"";if(bus)bus.textContent=btn.dataset.business||"";}));}
const arcBtns=$$(".architecture-branches button");if(arcBtns.length){const outB=$(".architecture-output b");const outP=$(".architecture-output > div:nth-child(2) p");const outE=$(".architecture-output > div:nth-child(3) p");const outBus=$(".architecture-output > div:nth-child(4) p");arcBtns.forEach((btn)=>btn.addEventListener("click",()=>{arcBtns.forEach((b)=>b.classList.toggle("active",b===btn));if(outB)outB.textContent=btn.dataset.title||"";if(outP)outP.textContent=btn.dataset.what||"";if(outE)outE.textContent=btn.dataset.experience||"";if(outBus)outBus.textContent=btn.dataset.business||"";}));}
const dialog=$(".summary-dialog");$$(".summary-open").forEach((btn)=>btn.addEventListener("click",()=>dialog?.showModal()));dialog?.querySelector(".dialog-close")?.addEventListener("click",()=>dialog.close());dialog?.addEventListener("click",(e)=>{if(e.target===dialog)dialog.close();});})();