
Deploy note: staging config must live OUTSIDE this tree (../next-config.local.php relative to the deploy folder). The in-tree avos-php/config.local.php is a convenience copy only — deploys replace this tree.
