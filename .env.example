# Frontend environment (build-time; NEVER put real secrets here)
# API origin for the AV OS PHP backend. Leave empty for same-origin (production).
VITE_API_BASE=
# Dev-only: where `npm run dev` proxies /api, /admin, /media
VITE_DEV_API_PROXY=http://127.0.0.1:8093
